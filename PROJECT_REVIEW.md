# 安居客全国小区数据引擎 — 全案评估报告

> **日期**: 2026-09-07
> **性质**: 起因/经过/结果 + 已知风险难点汇总(供整体评估决策)
> **关联**: README.md(操作手册, 截至 2026-08-06) | 本次为 2026-09-01~09-07 会话后的全案回顾

---

## 一、起因(这个项目在解决什么问题)

**目标**: 覆盖全国 ~100 重点城市的小区数据(基础信息/坐标/周边配套/价格趋势/业主评价), 作为 LLM 自动写**探盘文案**(real-estate-marketing)的数据底座。

**本质矛盾**: 房产营销内容需要海量真实、结构化、带坐标的社区数据支撑; 安居客是国内最完整的社区数据源(官方属性: 名称/地址/商圈/坐标/价格/户型/周边), 但无公开 API → 只能爬。

**起点**: 2026-07 从杨凌 149 小区起步, 逐城扩展 → 演变成一套覆盖爬取/存储/校验/双机协同的完整数据引擎。

---

## 二、经过(演进路线)

| 阶段 | 时间 | 内容 |
|------|------|------|
| **起步探索** | 2026-07 | 杨凌 149 → 摸清安居客 URL 两套格式/翻页限制/验证码 |
| **架构定型** | 2026-07 中 | session 工厂(本地可见 Chrome 过码)+ 双机并行(本地 + CubeMini 住宅IP)+ per-city 会话隔离 + `--shangquan all` 按商圈爬 |
| **四阶段补全** | 07-31~08-04 | 详情修复(844)/北京新区(1,607)/商圈网页回填/fill_shangquan 众数 |
| **省市层级** | 2026-08-06 | provinces(34)+cities(338) 权威层级; 修跨城商圈 LIKE 通配符 bug; district_id slug 统一 |
| **整市重爬三大城** | 08-28~30 | CubeMini 换新 IP(120.12.31.54)= 全新风控, 上海/广州/深圳 + 北京/成都/西安大规模重爬, 数据 47,968 → 121K |
| **B4 商圈重爬** | 09-01~03 | 三城按商圈遍历回填 shangquan_id, 缺商圈 3,672 → 107; 发现并修复分页死循环 |
| **B1 详情补** | 09-04~07 | B4 发现的新社区补详情, 4,090 → 1; 修复跨城 shangquan 匹配 bug |

**期间沉淀的架构决策**(详见 README):
- 双 IP 策略 + per-city storage_state 会话隔离
- 可见 Chrome `--show` 100% 可靠 vs headless 秒触发验证码
- 按商圈爬(最小单元)而非按区爬(翻页上限)
- 校验体系: validate.py 四维×两级 + check_crawl.py + 对抗测试

---

## 三、结果(2026-09-07 现状)

### 数据规模
```
总小区: 125,525   有详情: 100.00%(125,524/125,525)
缺坐标: 1(雁荡大厦)   空名称: 0   缺商圈: 1,006(含 899 诚实空值)
```

### 各城覆盖
| 城市 | 区 | 小区 | 城市 | 区 | 小区 |
|------|-----|------|------|-----|------|
| chengdu | 25 | 23,895 | shenzhen | 1 | 13,031 |
| shanghai | 2 | 37,636 | xa | 8 | 7,254 |
| beijing | 17 | 21,848 | datong | 1 | 1,021 |
| guangzhou | 1 | 19,941 | langfang | 3 | 750 |
| yangling | 1 | 149 | | | |

### 详情字段填充率
| 字段 | 覆盖 | 说明 |
|------|------|------|
| surrounding_json(周边配套) | 99.0% | 稳定 |
| community_review(业主评价) | 67.5% | 源数据上限 |
| price_trend_json(价格走势) | 66.9% | 源数据上限 |
| **property_basic_json**(基础信息) | **62.9%** | 存量 45K 用旧解析器未爬 |
| **community_interpretation_json**(小区解读) | **27.6%** | 源上限 ~44% + 存量 |
| huxingtu_json(户型) | 0.1% | 独立页, 未全量爬 |

### 校验器状态
- **BLOCK 0** ✅(结构/GB/爬取契约/无跨城商圈全过)
- WARN 3: B1/B2=雁荡大厦 1 条(验证码墙), B4=107(诚实空值), B3=0

### 本次会话(09-01~07)成果
- G4 districts.type 118 条回填; B3 空名称 103 条修复(`--fix-names` 模式)
- B4: shenzhen 1,396→27 / guangzhou 1,156→10 / shanghai 1,105→42(全商圈遍历收官)
- B1: 新增跨市社区 4,090 条详情补齐(→剩雁荡大厦 1)
- 修复 3 个真实 bug: 分页死循环(92页)、跨城 shangquan 匹配(城市限定)、district.type 空列
- 沉淀: resume_b4.sh / run_b1_details.sh(冷却+崩溃续跑) / fix_district_type.py

---

## 四、已知风险与难点(重点)

### ⚠️ A. 反爬 / 风控(最不稳定的外部变量)
1. **易盾验证码墙**: 触发条件不明, 有 IP 疲劳周期——连续爬 8.5h 后频率从 1次/3h 升到 1次/5min; headless 完全无法过; VL 模型仅对文字类有效, 滑块/点击型无效(实测 3 轮失败)。
2. **IP 信誉是稀缺且不可再生的资源**: 本地 IP 曾深度限流(全站验证码墙); CubeMini 2026-08-28 换公网 IP 才恢复零验证码高速爬。**换 IP = 重置信誉, 但每次换都有人工成本**(跳板机/登录/过码)。
3. **同 IP 双进程/双 Chrome 显著加剧验证码** —— 并行爬取有硬约束。
4. **冷却有效但非万能**: 30-60min 冷却可降频率; 但若验证码墙已形成, 需小时级休息, 无法预测。
5. **页面结构变化 = 全量解析失效风险**: 爬虫大量依赖 innerText 正则, 安居客一次改版可能让周边/解读/基础信息解析全部失配, 需持续维护。

### ⚠️ B. 数据质量(影响 LLM 产出可信度)
1. **跨市盘污染(本次 B4 放大)**: 各城"周边"桶把异地社区(东莞→shenzhen、佛山/清远→guangzhou、嘉兴/苏州→shanghai 等 ~4,000+)混入主城库, `region_id` 归主城、商圈归主城周边。**风险**: LLM 写"深圳"文案可能引用到东莞社区的数据。**现状**: 无标记字段区分"主城社区 vs 周边跨市盘", 治理策略未定。
2. **行政区划/商圈映射不完美**:
   - districts.name 早期中文/slug 混用(已修 district_id, 但 name 列仍部分存英文 slug, 如 datong/xa)。
   - Phase 4 区域众数填充的 ~12K 条商圈 ID 是**近似值**(非按商圈实际遍历), 精确度未知(本次 B4 已部分修正主城, 但 chengdu/xa/beijing 存量可能仍含近似值)。
   - 功能区(高新/曲江/浐灞等 type='功能区')的商圈结构与安居客实际是否对齐**未验证**。
3. **源数据本身不全**: interpretation(小区解读)安居客仅 ~44% 小区有经纪人内容; huxingtu(户型)数据不全——**这类字段无论怎么爬都有天花板**, LLM 文案可能缺"人味"素材。
4. **历史解析器代差**: property_basic/interpretation 是 08-11 才加入爬虫, 07 爬的 ~45K(成都/西安/北京/杨凌)用旧代码 → 需重爬才能补, 重爬 45K ≈ 数周。
5. **正则 + innerText 的脆弱性**: 长文本截断(interpretation 单段 >600 字被丢)、页面懒加载(tab 未点击则不出现内容)、排版微变即漏抓——样本实测已发现部分情况, 解析器天花板未完全探明。
6. **坐标质量未抽查**: 仅校验"非零", 未验证反代/漂移精度; 1 条缺坐标(雁荡大厦)持续验证码墙。

### ⚠️ C. 架构 / 运营(工程侧脆弱点)
1. **本地 Mac 是单点 + 人在回路依赖**: 它是会话工厂+主爬机, `--show` 需人工过码。**人不在 = 验证码白等 10min/次**(本次 09-01~07 大量无人值守超时, 拖慢数倍); CubeMini 虽是住宅 IP 但 headless 无法独立创建会话, 依赖本地同步 state。
2. **双机 DB 分裂**: 本地 125,525 vs CubeMini ~55,906, 本地为权威库但**没有可靠的持续同步机制**, 合并/迁移复杂(列交集容错、merge_detail、元数据保留)。
3. **长跑崩溃常态**: Playwright `TargetClosedError` 每 ~8h 出现(浏览器被关), 单进程跑大城必崩; 本次靠 resume 循环兜底, 但每次重启有冷却损耗。
4. **状态/会话生命周期是隐性依赖**: 每个城市一个 storage_state 文件, 过期/跨机/IP 突变即触发验证码; Chrome profile 信誉是时间积累, 误删归零。**这类状态无统一监控**。
5. **DB 是 sqlite 单文件 ~1.3G+**: 备份 7 天滚动尚可, 但并发写(双机)/大查询/崩溃恢复缺乏机制; 已出现 WAL 遗留(0B .db-wal 文件)。

### ⚠️ D. 业务 / 商业(超出爬虫本身)
1. **文案质量与数据主观字段天花板**: 探盘文案的"人味"(解读/点评/亮点)受限于源数据只有 ~44% 解读 + 评价稀疏, 可能需要 LLM 补写 → 有编造成本与合规风险。
2. **时效性**: 房价/挂牌/在售数量是快照, 非实时; 用于营销文案需标注时间或定期刷新(刷新 = 持续 IP 消耗)。
3. **合规边界**: 大规模爬取商业网站违反其 ToS 的法律风险未评估(当前是个人/运营用途, 但扩展前应明确)。
4. **扩展成本曲线**: 全国 100 城 = 每城需 discover_shangquan(人工过码一次)+ 数天~数周爬取 + IP 配额, **时间/IP 成本随城市数线性甚至超线性增长**, 且每城都要重建 IP 信誉或承担验证码风险。

### ⚠️ E. 本次会话暴露/待办的具体隐患
1. **跨城商圈 bug 存量排查**: 已修"光明"类名称跨城匹配(城市限定), 但**存量 125K 是否还有其他跨城/错配未全部复核**(13 条已清, 理论上解析路径已堵)。
2. **跨市盘的详情数据归属**: B1 补详情把东莞/佛山/嘉兴社区的完整详情爬进了主城库, 若后续决定清理跨市盘, 这些工作白做; 若保留, 需加标记字段防 LLM 误用。
3. **雁荡大厦**(125K 中唯一): 8-30 起持续验证码墙, 需人工一次 Chrome 过码。
4. **validate 只查了已知维度**: 同城"区不对"(如社区挂错区内商圈)未自动校验(README 待办 #7), 需按商圈重爬才精确。

---

## 五、一句话总结

> **数据底座已建成且干净**(125K 小区 / 详情 100% / 商圈 99% / BLOCK 全过), 但**爬取能力的天花板是反爬风控(IP 信誉 + 人工过码), 数据质量的天花板是源数据本身(解读/户型不全 + 跨市盘污染), 工程上最脆的是"本地单点 + 人在回路 + 会话状态隐性依赖"**。下一步价值最大且风险最低的方向是 LLM 文案生成(P2), 而非继续扩城/补齐(边际收益低、IP 成本高)。

---

*本文档随 README.md 并存: README 是操作手册(怎么做), 本文档是决策评估(值不值得/风险多大)。*
